# Example configuration file for loku
# Copy this file to config.toml and customize as needed.
#
# Env-var names follow loku_<flag> in lowercase (e.g. loku_library, loku_listen).

# Logging configuration
log_level = "info"  # Options: trace, debug, info, warn, error
log_format = "text"  # Options: text, json

# Address to listen on
# listen = "127.0.0.1:3000"       # TCP: host:port
# listen = "/run/loku/loku.sock"  # Unix domain socket path
# listen = "sd-listen"            # Inherit socket from systemd (socket activation)

# Library roots (at least one required).  Each entry names a directory tree
# holding one dataset kind: "downloads" for yt-dlp style downloads with
# info.json sidecars, "discs" for MakeMKV disc rips.  The name becomes the
# /files/{name} URL segment, so it must be lowercase ASCII letters, digits,
# '_', or '-'.
#
# [[library]]
# name = "downloads"
# path = "/media/videos"
# kind = "downloads"
#
# [[library]]
# name = "discs"
# path = "/media/rips"
# kind = "discs"
#
# Alternatively, --library PATH on the command line configures a single
# downloads-kind library (developer convenience; overrides [[library]]
# entries).

# Directory for persistent server state (the media index database).
# Defaults to systemd's STATE_DIRECTORY when present, else
# $XDG_STATE_HOME/loku, else ~/.local/state/loku.
# state_dir = "/var/lib/loku-server"

# Base URL of the service.  Only needed for OIDC deployments; defaults to a
# local address otherwise.
# base_url = "https://loku.example.com"

# OIDC authentication (optional).  Set all three to require login; the secret
# file may also be supplied via systemd LoadCredential.
# oidc_issuer = "https://sso.example.com/application/o/loku"
# oidc_client_id = "loku"
# oidc_client_secret_file = "/run/credentials/loku.service/oidc-secret"
